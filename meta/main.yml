---
galaxy_info:
  author: Balanced
  description: Ansible role to install Balanced's devpi
  license: MIT
  min_ansible_version: 1.4
  platforms:
    name: Ubuntu
    version:
      - precise
  categories: []

dependencies:

  - {role: 'git+git@github.com:zenoamaro/ansible-supervisord.git,master,supervisord' }
